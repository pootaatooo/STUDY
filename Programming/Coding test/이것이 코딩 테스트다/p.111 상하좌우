#include <iostream>

using namespace std;

int main() {
    int n;
    cin >> n;
    char c;
    int x = 1, y = 1;
    
    while(cin >> c) {
        if(c == 'R') {
            x++;
            if(x == 0 || x > n) {
                x--;
                continue;
            }
        }
        else if(c == 'L') {
            x--;
            if(x == 0 || x > n) {
                x++;
                continue;
            }
        }
        else if(c == 'U') {
            y--;
            if(y == 0 || y > n) {
                y++;
                continue;
            }
        }
        else if(c == 'D') {
            y++;
            if(y == 0 || y > n) {
                y--;
                continue;
            }
        }
        
    }
    
    cout << y << ' ' << x;
    
}

단순하게 nxn 정사각형 공간에서 0, n+1 이 되는 경우, 즉 주어진 공간을 벗어나는 경우를 제외하고
x값 y값을 연산해서 출력해줬다.
