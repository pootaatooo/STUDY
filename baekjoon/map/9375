#include <iostream>
#include <map>

using namespace std;

int main() {
    int t;
    cin >> t;
    int n;
    
    for(int i=0;i<t;i++) {
        cin >> n; 
        map<string,int> m; // 반복문을 돌때마다 초기화 시켜줌
        string x, y;
        
        for(int j=0;j<n;j++) {
            cin >> x >> y;
            m.insert({y,j++});
        }
        
    }
    
    
}
