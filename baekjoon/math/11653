#include <iostream>
using namespace std;

int yout(int a) {
    for(int i = 2; i*i <= a; i++) {
        if(a % i == 0)  return 0;
    }
    return a;
}

int main() {
    int n;
    int so;
    cin >> n;
    
    if(n == 1)
        return 0;
    
    for(int i = 2; i < n; i++) {
        
        if(yout(i) != 0)
            so = yout(i);
        
       !!!!! if(n % so == 0) {
            cout << so << '\n';
        }
        
    }
    
}
